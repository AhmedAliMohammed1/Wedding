# Decorations

The invitation’s botanical decorations are intentionally rendered with lightweight CSS and React components. Add optimized replacement SVG files here if you choose to use custom illustrations.
