/**
 * Migration tombstone for older project copies.
 *
 * The dress-code section is intentionally removed from the invitation.
 * Keeping this empty module ensures extracting a newer source package over an
 * older folder replaces the obsolete component instead of leaving it behind.
 */
export {};
